# StandX MM Bot Configuration / StandX MM Bot 設定ファイル

# ========================================
# Authentication (Required) / 認証設定（必須）
# ========================================
STANDX_PRIVATE_KEY=your_private_key_here
STANDX_WALLET_ADDRESS=your_wallet_address_here
STANDX_CHAIN=bsc

# ========================================
# Trading Settings / 取引設定
# ========================================
SYMBOL=ETH_USDC
ORDER_SIZE=0.1

# ========================================
# Distance Settings (in bps) / 距離設定（bps単位）
# ========================================
# Target distance from mark price (must be between 0 and 10)
# mark_priceからの目標距離（0～10の範囲）
TARGET_DISTANCE_BPS=8.0

# Escape threshold - move order when price gets this close
# 約定回避の閾値（価格がこの距離まで近づいたら逃げる）
ESCAPE_THRESHOLD_BPS=3.0

# Distance to move when escaping
# 逃げる先の距離
OUTER_ESCAPE_DISTANCE_BPS=15.0

# Reposition when distance exceeds (10 - this value)
# 再配置の閾値（距離が10 - この値を超えたら再配置）
REPOSITION_THRESHOLD_BPS=2.0

# Reposition when price moves by this amount
# 価格変動による再配置の閾値
PRICE_MOVE_THRESHOLD_BPS=5.0

# ========================================
# Connection Settings / 接続設定
# ========================================
# WebSocket reconnect interval in milliseconds
# WebSocket再接続間隔（ミリ秒）
WS_RECONNECT_INTERVAL=5000

# JWT expiration in seconds (default: 7 days)
# JWT有効期限（秒、デフォルト: 7日間）
JWT_EXPIRES_SECONDS=604800
