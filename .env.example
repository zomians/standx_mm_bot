# ===== 認証設定 =====
# ウォレット秘密鍵 (NEVER COMMIT THIS!)
# BSC: 0x付き64文字hex（secp256k1）
# Solana: 64文字hex、0xプレフィックスなし（Ed25519）
STANDX_PRIVATE_KEY=0x...

# ウォレットアドレス
# BSC: 0x付き40文字hex（Ethereum互換）
# Solana: Base58形式
STANDX_WALLET_ADDRESS=0x...

# チェーン (bsc or solana)
# BSC: make wallet-bsc で生成
# Solana: make wallet-solana で生成
STANDX_CHAIN=bsc

# APIリクエスト署名用Ed25519秘密鍵（BSC専用）
# BSC: make wallet-bsc で自動生成される
# Solana: 不要（ウォレット秘密鍵を使用）
STANDX_REQUEST_SIGNING_KEY=

# ===== 取引設定 =====
# 取引ペア（ハイフン区切り形式: BTC-USD, ETH-USD など）
SYMBOL=ETH-USD

# 片側注文サイズ
# 推奨: 0.001 で固定（増やす必要がない限り変更不要）
ORDER_SIZE=0.001

# ===== 距離設定 (bps) =====
# 目標距離 (10bps境界から内側)
TARGET_DISTANCE_BPS=8.0

# 約定回避距離 (この距離まで近づいたら逃げる)
ESCAPE_THRESHOLD_BPS=3.0

# 逃げる先の距離
OUTER_ESCAPE_DISTANCE_BPS=15.0

# 10bps境界への接近しきい値 (再配置トリガー)
REPOSITION_THRESHOLD_BPS=2.0

# 価格変動による再配置しきい値
PRICE_MOVE_THRESHOLD_BPS=5.0

# ===== 接続設定 =====
# WebSocket再接続間隔 (ms)
WS_RECONNECT_INTERVAL=5000

# JWT有効期限 (秒, デフォルト7日)
JWT_EXPIRES_SECONDS=604800

